# Base URL of the FintechAPI backend (see ../FintechBackend).
# Run the backend with the "https" launch profile, then trust the local dev cert once:
#   dotnet dev-certs https --trust
VITE_API_BASE_URL=https://localhost:7026/api
